<?xml version="1.0" encoding="utf-8"?>
<resources>


    <style name="Theme.CleanSync" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Theme -->
    <style name="Theme.CleanSync.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background_light</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_cleansync_logo</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.CleanSync</item>
    </style>

</resources>